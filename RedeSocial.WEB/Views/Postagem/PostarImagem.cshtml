@model RedeSocial.WEB.Models.PostagemViewModel
@{
    ViewBag.Title = "Upload";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Postar Imagem</h2>
<br />

@using (Html.BeginForm("PostarImagem", "Postagem", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @*<fieldset class="form-horizontal">
        <div class="form-group">
            <label class="control-label col-md-2" for="Photo">Imagem</label>
            <div class="col-md-10">
                <input class="glyphicon glyphicon-upload" type="file" name="photo" required />
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-md-2" for="Conteudo">Conteudo</label>
            <div class="col-md-10">
                <input class="form-control" type="text" name="Conteudo" placeholder="Em que voce esta pensando ?" required />
                <textarea class="form-control" placeholder="Update your status" name="Conteudo" required></textarea>
            </div>
        </div>

        <div class="form-group">

            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Submit" class="btn" />
            </div>

        </div>
    </fieldset>*@

    <div class="well">
        <form class="form-horizontal" role="form">
            <h4>What's New</h4>
            <div class="form-group" style="padding:14px;">
                <textarea class="form-control" placeholder="Update your status" name="Conteudo"></textarea>
            </div>
         

            <div class="fileUpload btn btn-primary">
                <span>Upload</span>
                <input type="file" class="upload" name="photo" />
            </div>

            <button class="btn btn-primary pull-right" type="submit">Post</button>

   
          
        </form>
    </div>



}  